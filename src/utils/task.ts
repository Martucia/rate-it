import { commonSlice } from "../store/reducers/commonSlice";
import { AppDispatch } from "../store/store";

// export const openTask = (id: number, isOpen: boolean) => async (dispatch: AppDispatch) => {
//     try {
//         dispatch(commonSlice.actions.toggleTaskModal({ id, isOpen }));
//     } catch (e: any) {
//         console.log(e)
//     }
// }